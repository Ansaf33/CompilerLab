begin

read(a);
read(b);
read(c);

if ( a <= b ) then
  if ( a <= c ) then
    write(a);
  endif;
endif;

if ( b <= a ) then
  if ( b <= c ) then
    write(b);
  endif;
endif;

if ( c <= a ) then
  if ( c <= b ) then
    write(c);
  endif;
endif;

end;
